import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
    palette: {
        secondary: {
            main: '#b389f5'
        },
        primary: {
            main : '#000000'
        },
        background: {
            default: '#0c0c0c'
        }
    }


})

export default theme;

